# Frontend (React + TypeScript)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Environment:

1. `VITE_GATEWAY_HTTP_BASE` (default: `http://localhost:8080`)
2. `VITE_GATEWAY_WS_BASE` (default: `ws://localhost:8080/ws`)
